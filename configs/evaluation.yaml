# Provisional thresholds. Calibrate all values on a human-annotated validation set
# before reporting formal experiments.
content_threshold: 0.75
style_removal_threshold: 0.60
identity_threshold: 0.65
use_identity_threshold: false
